# Cloudflare ingress-ready Docker environment template.
#
# Usage:
#   cp .env.cloudflare.example .env
#   replace INGRESS_SIGNING_SECRET below in both the backend and Worker
#   docker compose up -d --build --wait --wait-timeout 240

COMPOSE_PROJECT_NAME=all-mail
APP_PUBLISH_HOST=127.0.0.1
APP_PORT=3002
APP_INTERNAL_PORT=3000
LEGACY_API_INTERNAL_PORT=3100
REDIS_PUBLISH_HOST=127.0.0.1
REDIS_PORT=6380
REDIS_INTERNAL_PORT=6379
POSTGRES_PUBLISH_HOST=127.0.0.1
POSTGRES_PORT=15433
POSTGRES_INTERNAL_PORT=5432
POSTGRES_USER=allmail
POSTGRES_PASSWORD=allmail_dev_password
POSTGRES_DB=allmail

# Go public API runtime.
# bridge: require protocol-valid PostgreSQL, Redis, and compatibility API readiness.
# static: serve only the built React SPA; backend routes return 503 until ported.
GO_API_MODE=bridge
READY_TIMEOUT_SECONDS=5
SHUTDOWN_TIMEOUT_SECONDS=15
WORKER_HEARTBEAT_SECONDS=15
WORKER_HEARTBEAT_MAX_AGE_SECONDS=90

# App bootstrap secrets (optional for first boot).
# The one-shot legacy-init service generates and persists missing values before
# the API and Go workers start.
JWT_SECRET=
ENCRYPTION_KEY=
ADMIN_USERNAME=admin
ADMIN_PASSWORD=

PUBLIC_BASE_URL=
ALL_MAIL_STATE_DIR=/var/lib/all-mail
ALL_MAIL_PRINT_BOOTSTRAP_PASSWORD=false

# Emergency compatibility switch for a reviewed legacy P3005 repair only.
# Keep false during normal production startup.
ALL_MAIL_ALLOW_LEGACY_DB_PUSH_REPAIR=false

CORS_ORIGIN=
ALLOW_LOCAL_RATE_LIMIT_FALLBACK=false
JWT_EXPIRES_IN=2h
ADMIN_LOGIN_MAX_ATTEMPTS=5
ADMIN_LOGIN_LOCK_MINUTES=15
ADMIN_2FA_SECRET=
ADMIN_2FA_WINDOW=1

# Go retention worker.
API_LOG_RETENTION_DAYS=30
API_LOG_CLEANUP_INTERVAL_MINUTES=60
API_LOG_CLEANUP_RETRY_SECONDS=30
API_LOG_CLEANUP_TIMEOUT_SECONDS=60
API_LOG_CLEANUP_BATCH_SIZE=5000

# Go forwarding worker.
FORWARDING_WORKER_INTERVAL_SECONDS=30
FORWARDING_WORKER_BATCH_SIZE=10
FORWARDING_RUN_TIMEOUT_SECONDS=120

DOMAIN_BOOTSTRAP_ADMIN_USERNAME=
DOMAIN_BOOTSTRAP_ADMIN_PASSWORD=
SEND_ENABLED_DOMAINS=

# Optional ingress signing secret, shared with the Cloudflare Worker.
INGRESS_SIGNING_SECRET=replace-with-random-ingress-secret
INGRESS_ALLOWED_SKEW_SECONDS=300

# Optional provider OAuth fallback. Database-managed provider configuration takes precedence.
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_REDIRECT_URI=http://127.0.0.1:3002/admin/oauth/google/callback
GOOGLE_OAUTH_SCOPES=openid email profile https://www.googleapis.com/auth/gmail.modify https://mail.google.com/
MICROSOFT_OAUTH_CLIENT_ID=
MICROSOFT_OAUTH_CLIENT_SECRET=
MICROSOFT_OAUTH_REDIRECT_URI=http://127.0.0.1:3002/admin/oauth/outlook/callback
MICROSOFT_OAUTH_TENANT=consumers
MICROSOFT_OAUTH_SCOPES=offline_access openid profile email https://graph.microsoft.com/User.Read https://graph.microsoft.com/Mail.ReadWrite https://graph.microsoft.com/Mail.Send https://graph.microsoft.com/Contacts.ReadWrite https://graph.microsoft.com/Calendars.ReadWrite https://graph.microsoft.com/MailboxSettings.ReadWrite
